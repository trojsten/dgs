\color{(* colocolor or 'white' *)} \tiny \hfill
(* competition.name *), volume (* competition.volume *): booklet
built by (* build.user *) with dgs \texttt{(* build.dgs.branch *)}: \texttt{(* build.dgs.hash *)}
at (* build.timestamp.strftime('%Y-%m-%d %H:%M:%S') *)
from branch \texttt{(* build.repo.branch *)}: \texttt{(* build.repo.hash *)}

