% DeGeŠ style file
% module: seminar

\fancypagestyle{first-(* competition.id *)-(* volume.id *)}{%
    \pagestyle{first-(* competition.id *)}
    \fancyhead[C]{
        \begin{minipage}{0.7\textwidth}
            \centering
            \vspace{4pt}
            {\Large (* competition.full.nominative *)}\\[4pt]
            {\large (* volume.number|roman *). ročník, (* volume.number + competition.founded - 1 *)/(* volume.number + competition.founded *)}\\[8pt]            
            {\footnotesize (* competition.short *), (* competition.organisation.department.short *) (* competition.organisation.faculty.short *), (* competition.organisation.address *)}\\[2pt]
            \vspace{6pt}
        \end{minipage}        
    }           
}  

\fancypagestyle{main-(* competition.id *)-(* volume.id *)}{%
    \pagestyle{main-(* competition.id *)}
}
