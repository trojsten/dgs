\documentclass[12pt, twoside]{article}

\usepackage{core/dgs}
\usepackage{core/config/dgs-config}
\RequirePackage{modules/seminar/seminar}

\loadDocumentSettings
\usepackage{core/dgs-custom}

\newcounter{problem}

\begin{document}            
    \loadRegularStyle
	\thispagestyle{first}
	\vspace*{2em}
   
    \section{\texorpdfstring{Riešenia (* round.id *). kola (* semester.genitive *) časti}{Riešenia}}
        
    \pagestyle{problems}    
	\loadSolutionsStyle
    %% for problem in round.problems
    \addSeminarSolution{(* seminar.id *)}{(* volume.id *)}{(* semester.id *)}{(* round.id *)}{(* problem.id *)}{(* problem.title *)}{(* problem.solutionBy *)}{(* problem.evaluation *)}{(* problem.genderSuffix *)}
    %% endfor
\end{document}
