\documentclass[12pt, twoside]{article}

\usepackage{core/dgs}
\usepackage{core/config/dgs-config}
\RequirePackage{modules/seminar/seminar}

\loadDocumentSettings
\usepackage{core/dgs-custom}

\newcounter{problem}
 
\begin{document}
    \loadRegularStyle
    \thispagestyle{first}
    \vspace*{2em}

    {
        \section{\texorpdfstring{Zadania (* round.id *). kola (* semester.genitive *) časti}{Zadania}}
        \centering
        \vspace{-1.3em}
        Termín odoslania (* round.deadline.strftime('%d. %m. %Y') *)\\[2pt]
    } 
        
    \pagestyle{problems}    
    \loadProblemsStyle
    %% for problem in round.problems
    \addSeminarProblem{(* seminar.id *)}{(* volume.id *)}{(* semester.id *)}{(* round.id *)}{(* problem.id *)}{(* problem.title *)}{(* problem.pointsDescription *)}{(* problem.pointsCode *)}
    %% endfor
\end{document}
