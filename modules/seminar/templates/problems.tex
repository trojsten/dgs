\documentclass[12pt, twoside]{article}

\RequirePackage{core/dgs}
\RequirePackage{core/config/dgs-config}
\RequirePackage{core/dgs-custom}
\input{input/(* seminar.id *)/(* volume.id *)/(* semester.id *)/(* round.id *)/seminar.sty}

\newcounter{volume}
\newcounter{semester}
\newcounter{round}
\newcounter{problem}
 
\begin{document}
    \loadRegularStyle
    \thispagestyle{first}
    \vspace*{2em}

    {
        \section{\texorpdfstring{Zadania (* round.id *). kola (* semester.genitive *) časti}{Zadania}}
        \centering
        \vspace{-1.3em}
        Termín odoslania (* round.deadline.strftime('%d. %m. %Y') *)\\[2pt]
    } 
        
    \pagestyle{problems}    
    \loadProblemsStyle

    %% for problem in round.problems
    \setcounter{volume}{(* volume.id *)}%
    \setcounter{semester}{(* semester.id *)}%
    \setcounter{round}{(* round.id *)}%
    \setcounter{problem}{(* problem.id *)}%
    \RenewDocumentCommand{\problemDirectory}{}{\rootDirectory/(* seminar.id *)/(* volume.id *)/(* semester.id *)/(* round.id *)/(* problem.id *)}%
    \subsection{%
        \texorpdfstring{\large \textbf{(* problem.title *)} \normalsize\hfill\quad{(* problem.categories|formatCategories *)}}{(* problem.title *)}%
    }%
    \protectedInput{\problemDirectory/problem.tex}%
    %% endfor
\end{document}
