(@ extends 'homework-students.tex' @)

(@ block solution @)
    (@ if issue.problems|length == 0 @)
        \paragraph{Riešenie}
        \protectedInput{\activeDirectory/(* problem.id *)/solution.tex}
    (@ endif @)
(@ endblock solution @)

(@ block subsolution @)
    \paragraph{Riešenie}
    \protectedInput{\activeDirectory/(* problem.id *)/(* subproblem.id *)/solution.tex}
(@ endblock subsolution @)
