(@ extends 'handout-students.tex' @)

(@ block solution @)
    \paragraph{Riešenie}
    \setActive{\rootDirectory/(* module.id *)/(* course.id *)/(* year.id *)/(* self.typedir() *)/(* issue.id *)/(* problem.id *)}%
    \protectedInput{\activeDirectory/solution.tex}
(@ endblock solution @)
