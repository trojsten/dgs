(@ extends 'handout-base.tex' @)

(@ block sections @)
    (@ for sid, section in issue.sections.items() @)
        \subsection{(* section.name *)}
        \protectedInput{\activeDirectory/(* section.id *)/text.tex}
        (@ for pid, problem in section.problems.items() @)
            \subsubsection{Úloha (* problem.number *)}
            \protectedInput{\activeDirectory/(* section.id *)/(* problem.id *)/problem.tex}
            \paragraph{Riešenie}
            \protectedInput{\activeDirectory/(* section.id *)/(* problem.id *)/solution.tex}
        (@ endfor @)
        (@ if not loop.last @)\newpage(@ endif @)
    (@ endfor @)
(@ endblock sections @)
