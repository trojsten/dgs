(@ extends 'base.tex' @)

(@ block type @)handout(@ endblock @)
(@ block typedir @)handouts(@ endblock @)

(@ block style @)
    \fancypagestyle{handout}{
        \pagestyle{scholar}
        \fancyhead[L]{\textit{Cvičenie č. (* issue.number *)}}
        \fancyhead[R]{\textit{(* issue.date.strftime('%d. %m. %Y') *)}}
    }
(@ endblock style @)

(@ block content @)
    \section{Cvičenie č. (* issue.number *)(@ block subtitle @)(@ endblock subtitle @)}
    (@ for sid, section in issue.sections.items() @)
        \section{(* section.name *)}
        \protectedInput{\activeDirectory/(* section.id *)/text.tex}
        \subsection{Úlohy}
        (@ for pid, problem in section.problems.items() @)
            \subsubsection{(* problem.id *)}
            \protectedInput{\activeDirectory/(* section.id *)/(* problem.id *)/problem.tex}
            (@ block solution @)(@ endblock solution @)
        (@ endfor @)
        (@ if not loop.last @)\newpage(@ endif @)
    (@ endfor @)
(@ endblock content @)
