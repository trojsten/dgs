(@ extends 'handout-base.tex' @)

(@ block problems @)
    \subsection{Úlohy}
    (@ if issue.problems @)
        (@ for problem in issue.problems @)
            \subsubsection{Úloha (* loop.index *)}
            \protectedInput{\activeDirectory/(* problem.id *)/problem.tex}
        (@ endfor @)
    (@ endif @)
(@ endblock problems @)
