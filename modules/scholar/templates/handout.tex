\documentclass[12pt, twoside]{dgs}

\input{input/scholar/(* course.id *)/static/format-override.tex}
\input{input/scholar/(* course.id *)/(* year.id *)/handout/(* lesson.id *)/format-handout.tex}
 
\begin{document}
    \pagestyle{handout}
    \setcounter{section}{(* lesson.number - 1 *)}
    \section{Cvičenie č. (* lesson.number *)}
    \input{input/scholar/(* course.id *)/(* year.id *)/handout/(* lesson.id *)/content.tex}
\end{document}
