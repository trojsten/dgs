\documentclass[12pt, twoside]{dgs}

\input{input/scholar/(* course.id *)/static/format-override.tex}
\input{input/scholar/(* course.id *)/(* year.id *)/handouts/(* issue.id *)/format-handout.tex}
 
\begin{document}
    \pagestyle{handout}
    \setcounter{section}{(* issue.number - 1 *)}
    \RenewDocumentCommand{\activeDirectory}{}{\rootDirectory/(* module.id *)/(* course.id *)/(* year.id *)/handouts/(* issue.id *)}%
    \section{Cvičenie č. (* issue.number *)}
    \input{input/scholar/(* course.id *)/(* year.id *)/handouts/(* issue.id *)/content.tex}
\end{document}
