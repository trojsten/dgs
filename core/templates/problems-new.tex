\documentclass[12pt, twoside]{article}

\usepackage{core/dgs}
\usepackage{core/config/dgs-config}

\loadDocumentSettings
\usepackage{core/dgs-custom}

\newcounter{problem}
 
\begin{document}
    \loadRegularStyle
	\thispagestyle{first}
	\vspace*{2em}

    {
        \section{\texorpdfstring{Zadania (* booklet.round.id *). kola (* booklet.part.genitive *) časti}{}}
		\centering
		\vspace{-1.3em}
        Termín odoslania (* booklet.round.deadline *)\\[2pt]
    } 
        
	\pagestyle{problems}    
	\loadProblemsStyle
    %% for problem in round.problems
        \addProblem{(* problem.name *)}{(* problem.pointsDescription *)}{(* problem.pointsCode *)}
    %% endfor
\end{document}
