"""
Recovering Náboj booklets that survive only as compiled PDFs.

`tools.ancient` proper converts TeX sources. Volumes 02-09 have no sources: the only
surviving copies are PDFs distilled between 2000 and 2006, whose text layer is damaged in
ways that are systematic and therefore reversible. Everything downstream of the decode --
`rules`, `units`, `figures`, `outlines`, the slug tables -- is the same machinery, because
these are the same authors writing the same notation as 2007.
"""
